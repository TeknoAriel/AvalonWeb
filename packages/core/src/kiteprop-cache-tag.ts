/** Tag compartido para `fetch(..., { next: { tags } })` del catálogo KiteProp; el cron llama `revalidateTag` con el mismo valor. */
export const KITEPROP_PROPERTY_FEED_TAG = 'kiteprop-property-feed';
