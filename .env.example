# Ejemplo de nombres de variables. Valores reales: terminal (`export …`) o `.env.local` (gitignored);
# no commitear secretos. Guía: docs/OPERACION.md → sección «Secrets locales».
# KiteProp (catálogo + CRM): ver docs/KITEPROP.md

# Funciones UI (coma). Ej: property_ask,recents
# NEXT_PUBLIC_DISABLED_FEATURES=
# Búsqueda en lenguaje natural en listados: apagada por defecto. Activar: NEXT_PUBLIC_ENABLE_NL_SEARCH=1

# Curadura extra listado Premier (opcional)
# PREMIER_MIN_GALLERY_IMAGES=1
# PREMIER_MIN_TOTAL_M2=40
# PREMIER_EXCLUDE_PROPERTY_IDS=123,456

# Puente server-to-server hacia herramientas tipo MCP (opcional). POST { tool, args } → { ok, result }
# KITEPROP_MCP_BRIDGE_URL=https://tu-bridge.example.com/mcp

# URL pública del sitio (cada proyecto Vercel)
# En local: localhost. En Vercel conviene la URL canónica del proyecto (p. ej. https://avalonweb.vercel.app).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# URLs cruzadas entre marcas (local)
NEXT_PUBLIC_AVALON_URL=http://localhost:3000
NEXT_PUBLIC_PREMIER_URL=http://localhost:3001

# Enlace explícito al otro sitio (opcional). Si no hay PEER ni PREMIER/AVALON URL, el código usa por defecto
# https://avalon-premier.vercel.app y https://avalonweb.vercel.app para enlaces cruzados.
# NEXT_PUBLIC_PEER_SITE_URL=http://localhost:3001

# WhatsApp en formato internacional sin +
NEXT_PUBLIC_WHATSAPP=543416811434

# Opcional: forzar IDs como Premier para QA (se excluyen del sitio estándar)
# PREMIER_PROPERTY_IDS=123,456
# NEXT_PUBLIC_PREMIER_PROPERTY_IDS=123,456

# Avalon Premier — Videos de portada (coma). Formas: VIDEO_ID | VIDEO_ID@segInicio-segFin | URL con start/end
# Ejemplo recorte 5s–30s: kSllrnwFhnA@5-30
# NEXT_PUBLIC_PREMIER_COVER_VIDEOS=kSllrnwFhnA@5-30

# Hero fullscreen (MP4 u origen compatible con <video>; si está vacío se usa imagen del feed)
# NEXT_PUBLIC_PREMIER_HERO_VIDEO_URL=https://ejemplo.com/hero-premier.mp4
# Imágenes curadas para el hero (coma o salto de línea). Completar `PREMIER_HERO_MANUAL_POSTERS` en apps/avalon-premier/lib/premier-hero-posters.ts es alternativa estática.
# NEXT_PUBLIC_PREMIER_HERO_IMAGE_URLS=https://…/1.jpg,https://…/2.jpg

# --- KiteProp API (servidor) — detalle en docs/KITEPROP.md ---
# Host sin /api/v1 (consultas). Con key: messages (ficha) / contacts (general).
# Catálogo GET /properties: prioridad KITEPROP_API_BASE_URL, si no está → KITEPROP_API_URL (mismo host con /api/v1 OK).
# KITEPROP_API_BASE_URL=https://www.kiteprop.com/api/v1
# KITEPROP_API_URL=https://www.kiteprop.com/api/v1
# KITEPROP_API_KEY=kp_...
# (solo local / legacy) KITEPROP_API_TOKEN=kp_...  — en Vercel usá solo KITEPROP_API_KEY; no dupliques.
# Opcional: ruta del listado si tu tenant usa otro path
# KITEPROP_API_PROPERTIES_PATH=/properties
# Filtro status en listado (default active)
# KITEPROP_API_STATUS_FILTER=active
# Por defecto el catálogo también pide active_unpublished (Premier). Para solo active:
# KITEPROP_API_NO_ACTIVE_UNPUBLISHED=1
# Tamaño de página API (10–100, default 50)
# KITEPROP_API_PER_PAGE=50
#
# Lista guardada “Premier” en KiteProp (propsheet, ej. …/saved/348/0138) — si la API incluye el id en el JSON:
# KITEPROP_PREMIER_SAVED_LIST_IDS=0138
# (alias un solo id) KITEPROP_PREMIER_PROPSHEET_ID=0138

# Opcional: URL completa de un POST legacy (sobrescribe messages/contacts; cuerpo plano full_name, etc.)
# KITEPROP_API_CONSULTA_URL=https://...

# --- BFF (Premier → Avalon Web): ver docs/OPERACION.md ---
# En Premier: si definís KITEPROP_API_KEY (misma que Web), el catálogo lee KiteProp **antes** que el BFF
# y el listado queda alineado con `pnpm kp:ingest-stats`. El BFF queda como respaldo si la API falla.
# Mismo Bearer que el cron: CRON_SECRET en ambos proyectos. Opcional legacy: INTERNAL_CATALOG_SECRET.
# AVALON_CATALOG_INTERNAL_URL=https://avalonweb.vercel.app/api/internal/catalog   # solo Premier
# Depuración (INSEGURO): sin Bearer en /api/internal/catalog — solo hasta verificar; luego borrar.
# CATALOG_INGEST_DEBUG=1

# Catálogo en runtime: GET /api/v1/properties + key; Premier: API primero o BFF. Sin key ni BFF → listado vacío (properties.json no es fallback).
# Opcional: User-Agent en requests server a KiteProp (Cloudflare); default ya es tipo navegador.
# Solo ASCII / Latin-1: no uses puntos suspensivos tipográficos (…) ni emojis (rompen fetch/undici).
# KITEPROP_FETCH_USER_AGENT=

# ISR: POST /api/revalidate?secret=... (solo servidor)
# REVALIDATE_SECRET=

# Cron Vercel (cada 2 h): GET /api/cron/refresh-catalog
# Mismo valor en Vercel (avalon-propiedades + avalon-premier). El cron manda: Authorization: Bearer <CRON_SECRET>
# Generar: openssl rand -hex 32
# CRON_SECRET=
